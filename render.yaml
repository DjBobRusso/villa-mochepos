services:
  - type: web
    name: villa-moche-sync
    env: node
    buildCommand: cd server && npm install
    startCommand: cd server && node server.js
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 3001